{
  "id": "#{id}",
  "name": "#{name}",
  "version": "0.1.0",
  "author": "#{author}",
  "description": [
    "#{description}"
  ],
  "category": "Gameplay",
  "gameVersion": "#{game_version}",
  "platform": "Quest",
  "components": [
    {
      "type": "HookMod",
      "installAction": {
        "installLibraryFile": "lib#{id}.so"
      },
      "uninstallAction": {
        "removeLibraryFile": "lib#{id}.so"
      }
    }
  ]
}
